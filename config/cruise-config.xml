<!-- config/cruise-config.xml -->
<cruise schemaVersion="139">
  <!-- The serverId attribute is removed to allow GoCD to auto-generate it on first run -->
  <server agentAutoRegisterKey="default">
    <security>
      <authConfigs>
        <authConfig id="password_file" pluginId="cd.go.authentication.passwordfile">
          <property>
            <key>PasswordFilePath</key>
            <value>/godata/config/password.properties</value>
          </property>
        </authConfig>
      </authConfigs>
      <roles>
        <role name="gocd-system-administrators">
          <users>
            <user>admin</user>
          </users>
        </role>
      </roles>
      <admins>
        <user>admin</user>
      </admins>
    </security>
  </server>

  <pipelines group="defaultGroup">
    <pipeline name="badminton_court">
      <materials>
        <git url="__GIT_REPO_URL_WITH_CREDENTIALS__" branch="master" />
      </materials>
      <stage name="build">
        <jobs>
          <job name="build_job">
            <resources>
              <resource>docker</resource>
            </resources>
            <tasks>
              <exec command="echo">
                <arg>Placeholder pipeline is running</arg>
              </exec>
            </tasks>
          </job>
        </jobs>
      </stage>
    </pipeline>
  </pipelines>
</cruise>
